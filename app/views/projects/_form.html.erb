<%= simple_form_for :project do |f| %>
	<%= f.input :name, label: "Project name" %>
	<%= f.input :finish_on, label: "Goal to finish the project (date)" %>

	<%= f.submit "Start Project", class: "radius button" %>
<% end %>