<%= simple_form_for @element do |f| %>

	<%= f.input :name, label: "Task name" %>
	<%= f.input :due_on, label: "Due date", as: :string %>

	<%= f.submit "Save task", class: "medium round button" %>

<% end %>